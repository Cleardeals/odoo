<odoo>
    <record id="ir_cron_expire_property_listings" model="ir.cron">
        <field name="name">Property Listings: Expire Services</field>
        <field name="model_id" ref="model_property_listing"/>
        <field name="state">code</field>
        <field name="code">model._cron_expire_listings()</field>
        <field name="interval_number">1</field>
        <field name="interval_type">days</field>
        <field name="active" eval="True"/>
    </record>
</odoo>